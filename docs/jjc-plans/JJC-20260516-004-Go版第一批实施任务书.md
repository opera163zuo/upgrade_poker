# JJC-20260516-004 Go版第一批实施任务书

> 项目基线: `opera163zuo/upgrade_poker` (Go语言仓库)
> 当前分支: `master` (commit `7e709f7`)
> 签署: 中书省

---

## 一、仓库核对报告

| 检查项 | 状态 |
|--------|------|
| 仓库路径 | `/home/zxyzxy/文档/upgrade_poker` |
| Git 分支 | `master` |
| HEAD | 已推进至 `7e709f7` |
| 工作区 | 干净（无未提交修改） |
| 语言 | **Go** 1.25.4, Ebitengine v2 GUI |
| 模块 | `github.com/smallnest/upgrade_poker` |
| 现有构建产物 | `release/upgrade_9cdaa16_linux64/win64` |
| 关键文件 | card.go / game.go / rules.go / ai.go / bid.go / player.go / trick.go / main.go |
| 测试文件 | game_test.go (32个测试) |
| 原模型 | 2副牌(108张), 每人25张, 底牌8张 |
| Makefile | build / test / release 目标完整 |

---

## 二、四副牌改造波及范围分析

基于完整代码审查，将4副牌改造分为 **5个批次**：

### 批次① ✅ 已实施 — 发牌模型改造
| 文件 | 函数/行 | 变更说明 |
|------|----------|----------|
| `card.go` | `Card.Copy` | 注释 0-1 → 0-3 |
| `card.go` | `NewDeck()` | 2副→4副, 108→216张, copy<2→copy<4 |
| `game.go` | `Deal()` | 25轮→52轮发牌 (52×4=208+8底=216) |
| `game.go` | `DealAnimated()` | 同上 |
| `game.go` | `PlayHand()` | TrickCount < 25 → < 52 |
| `game.go` | 注释 | "25 tricks" → "up to 52 tricks" |
| `game_test.go` | `TestNewDeck` | 108→216 |
| `game_test.go` | `TestDeal` | 25→52 per player, 108→216 total |
| `game_test.go` | `TestAIPlayNoCrash` | 25→52 |

### 批次② 出牌组合模型 — 三张/四张
| 文件 | 函数 | 变更说明 |
|------|------|----------|
| `rules.go` | `AnalyzePlay()` | 新增三级组合(3张SameRank)、四级组合(4张SameRank) |
| `rules.go` | `analyzeSameSuit()` | 同花色等级支持3张/4张 |

### 批次③ 拖拉机增强 — 三连/四连拖拉机
| 文件 | 函数 | 变更说明 |
|------|------|----------|
| `rules.go` | `findTractors()` | 支持同花色三个六三个七=拖拉机, 四个六四个七=拖拉机 |
| `rules.go` | `areConsecutiveRanks()` | 保留等级跳牌逻辑 |
| `rules.go` | `playType()` | 返回类型增加: triple=3, quad=4 |
| `rules.go` | `comparePlays()` | 四张大过三张和两张 |
| `rules.go` | `IsKillPlay()` | 四张可毙对子拖拉机, 不能毙三张拖拉机 |

### 批次④ 甩牌与跟出规则
| 文件 | 函数 | 变更说明 |
|------|------|----------|
| `rules.go` | `ResolveThrow()` | 甩牌失败→保留最小安全组件出牌,其余收回 |
| `rules.go` | `validateLeading()` | 三张/四张首出后手跟出规则 |
| `rules.go` | `validateFollowing()` | 首出三张时有对子必须跟出; 首出四张时有三张/对子/拖拉机都要跟 |
| `rules.go` | `countAvailablePairs()` | 三张/四张场景下对子计数 |

### 批次⑤ 亮主与升级规则
| 文件 | 函数 | 变更说明 |
|------|------|----------|
| `game.go` | `RunBiddingPhase()` | 无人亮主→翻底牌第一张花色作主牌 |
| `bid.go` | `BidType` | 新增 `BidQuadLevel` (4级牌亮主) |
| `bid.go` | `CanBid()` | 支持4张同花色级牌亮主 |
| `bid.go` | `CanOverrideBid()` | 不允许花色混合反主 |
| `game.go` | `HandleUpgrade()` | 等比比例替代固定分段升级阈值 |

---

## 三、第一批实施详情（已提交）

### 变更文件清单

**card.go** (9行变更)
- `Card.Copy` 注释: `0 or 1 for two-deck distinction` → `0-3 for four-deck distinction`
- `NewDeck()`: capacity `108→216`, loop `copy<2→copy<4`

**game.go** (8行变更)
- `Deal()`: `round < 25` → `round < 52` (52×4+8=216 ✓)
- `DealAnimated()`: same
- `PlayHand()`: `TrickCount < 25` → `TrickCount < 52`
- 注释: "25 tricks" → "up to 52 tricks"

**game_test.go** (14行变更)
- `TestNewDeck`: `108→216`
- `TestDeal`: `25→52` per player, `108→216` total
- `TestAIPlayNoCrash`: `TrickCount < 25→52`

### 证据链

```
$ go test -v ./...
→ 30/30 PASS (0.034s + 0.018s)

TestNewDeck:                     PASS (deck size=216)
TestDeal:                        PASS (52 per player, 216 total)
TestAIPlayNoCrash:               PASS (scores 225+175=400, 330+70=400, 150+250=400)

$ go build
→ success, 7.7MB

$ git log --oneline -1
7e709f7 feat: 四副牌发牌模型 — 216张/52手牌/8底牌 (JJC-20260516-004)

$ git push origin master
→ To github.com:opera163zuo/upgrade_poker.git
   74ba41a..7e709f7  master -> master
```

### 分数验证（四副牌总分=400）
- 第1局: Team0=225, Team1=175 → 225+175=400 ✓
- 第2局: Team0=330, Team1=70 → 330+70=400 ✓  
- 第3局: Team0=150, Team1=250 → 150+250=400 ✓

---

## 四、后续批次执行建议

### 推荐执行顺序

| 顺序 | 批次 | 预估难度 | 前置依赖 |
|------|------|----------|----------|
| 1 | 批次② 三张/四张组合检测 | 中 | 无 |
| 2 | 批次③ 拖拉机增强 | 中高 | 批次② |
| 3 | 批次⑤ 亮主翻底牌 | 低 | 无 |
| 4 | 批次④ 甩牌/跟出规则 | 高 | 批次②+③ |
| 5 | 批次⑤ 升级阈值等比比例 | 中 | 无 |
| 6 | UI适配52手牌布局 | 中 | 批次①完成 |

### 阻塞点清单
1. **UI布局**: 52手牌+8底牌=60张, 原UI为25张设计, 需重新计算slot布局
2. **三张/四张拖拉机定义**: 需确认跨等级牌是否算拖拉机
3. **甩牌处罚语义**: "最小安全组件"具体定义需前线确认
4. **升级等比比例**: 具体等比公式待确认

---

*文书归档: 中书省 · JJC-20260516-004 · 2026-05-16*
