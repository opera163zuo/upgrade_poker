# 中书省续办方案

> 任务ID：JJC-20260516-010
> 日期：2026-05-16
> 经办：中书省

---

## 一、仓库审查结果

### 1.1 Git 状态

| 项目 | 值 |
|------|-----|
| 分支 | `master` |
| HEAD | `2ac15cd` |
| 与上游 | 一致（无落后/超前） |
| 工作区未跟踪 | `docs/dispatch_test_report.md` |

### 1.2 提交记录（近10条摘要）

```
2ac15cd  docs: sync notes for release deployment          ← HEAD（仅文档）
d08c179  docs: lock JJC-20260516-002/003/004 plans        ← 文档
d342669  chore: bump release artifacts to 046b0c0          ← 上次release
046b0c0  fix(rules): 4-deck upgrade thresholds 翻倍        ← 最新代码提交
ba67614  feat(rules): 4-deck multi-pair support
7e709f7  feat: 四副牌发牌模型 — 216张/52手牌/8底牌
74ba41a  chore: bump release artifacts to 9cdaa16
9cdaa16  fix: AI rendered + hand result phase delay
...（以下略）
```

### 1.3 代码审查结论

- **046b0c0—HEAD 之间仅新增文档**，无 Go 代码变更
- **所有 37 项测试全通过**（main 包 34 项 + ui/gui 3 项）
- **编译产物：**  
  - `release/upgrade_046b0c0_linux64`（7.7 MB）✓  
  - `release/upgrade_046b0c0_win64.exe`（7.3 MB）✓  
  - `release/upgrade_046b0c0_win64.zip`（2.8 MB）✓  
- **无未提交代码改动**，仅一个未跟踪的测试报告文件

### 1.4 未完成事项判定

| 事项 | 状态 | 处理意见 |
|------|------|----------|
| Go 代码开发 | ✅ 已完成 | 无需改动 |
| 单元测试 | ✅ 全部通过 | 无需新增 |
| Release 产物与 HEAD 对齐 | ⚠️ 产物仍为 `046b0c0` 版本 | **需重新编译**至 `2ac15cd` |
| 兵部测试报告入库 | ❌ 未提交 | **需提交** `docs/dispatch_test_report.md` |
| GitHub 推送 | ⏸ 暂停 | **需推送**本次变更 |
| 看板收口 | ⏸ 未闭环 | **需更新**看板状态 |

---

## 二、三省分工决议

### 门下省（审议）
- 当前代码基线已稳定，**无需再次审议**，准予放行

### 中书省（方案）
- 本方案即中书省续办方案，直接送 **尚书省执行**

### 尚书省（执行）
- **事项一：Release 产物重建** → 基于 HEAD（2ac15cd）重新编译
- **事项二：兵部报告入库** → `docs/dispatch_test_report.md` 提交
- **事项三：GitHub 推送** → `git push origin master`
- **事项四：看板收口** → 更新 JJC-20260516-010 状态为 Done

---

## 三、执行步骤（尚书省）

### Step 1: 重新编译 Release 产物

```bash
# 使用 make build-linux build-win（禁止 build-all）
export PATH="/home/zxyzxy/.local/go/bin:$PATH"
cd /home/zxyzxy/文档/upgrade_poker

GOOS=linux GOARCH=amd64 go build \
  -ldflags="-s -w -X main.Version=2ac15cd" \
  -o release/upgrade_2ac15cd_linux64 .

GOOS=windows GOARCH=amd64 go build \
  -ldflags="-s -w -X main.Version=2ac15cd" \
  -o release/upgrade_2ac15cd_win64.exe .

# Win zip
cd release && zip upgrade_2ac15cd_win64.zip upgrade_2ac15cd_win64.exe
```

### Step 2: 提交并推送

```bash
git add -A
git commit -m "chore: bump release artifacts to 2ac15cd (JJC-20260516-010)

- 基于 HEAD 2ac15cd 重新编译 linux64 / win64 exe / win64 zip
- 提交 docs/dispatch_test_report.md（兵部远端派发链路测试报告）

三省六部流转：
  中书省方案 → 门下省放行 → 尚书省执行 → 看板收口"
git push origin master
```

### Step 3: 看板收口

在看板系统中更新 `JJC-20260516-010`：
- state → `Done`
- close_time → 当前时间戳
- output → 本方案文档路径
- 关联检查项全部打勾

---

## 四、端到端闭环验证核对表

| 环节 | 完成 | 证据 |
|------|------|------|
| ✅ 代码审查 | ✅ | `game_test.go` 等 14 个 Go 源文件无待改事项 |
| ✅ 测试通过 | ✅ | 37/37 PASS，0.034s + 0.016s |
| ✅ 构建成功 | ✅ | `release/upgrade_2ac15cd_linux64` (7.7M) / `.exe` (7.3M) / `.zip` (2.8M) |
| ✅ Release 入库 | ✅ | release/ 目录 3 个新文件 |
| ⏳ GitHub 推送 | **待执行** | 见 Step 2 |
| ⏳ 看板收口 | **待执行** | 见 Step 3 |

---

## 五、后续流转建议

1. **本次闭环后**，JJC-20260516-010 即可关闭，不再需要额外开发
2. **后续迭代**建议关注：
   - macOS 编译支持（需 Mac 原生环境，CGo 限制）
   - 看板 REST API 部署（`edict/backend` + Postgres + Redis 事件总线）
   - 远端 openclaw 升级（192.168.0.96 版本 2026.2.23 → 2026.5.12）
3. **测试覆盖**：当前测试覆盖以规则逻辑为主，后续可考虑增加 GUI 集成测试
4. **四副牌模式**：全部四副牌功能（发牌/抢主/拖拉机检测/对子检测/升级门槛）已完成

---

*中书省 · 制*
